package com.librarian.configuration;
/*
import com.librarian.dto.requestDto.save.MemberSaveRequestDto;
import com.librarian.model.Member;
import org.modelmapper.ModelMapper;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class ModelMapperConfig {

    @Bean
    public ModelMapper modelMapper() {
        ModelMapper modelMapper = new ModelMapper();

        modelMapper.typeMap(MemberSaveRequestDto.class, Member.class)
                .addMappings(mapper -> mapper.map(MemberSaveRequestDto::getAddresses, Member::setAddress));

        return modelMapper;
    }
}
*/